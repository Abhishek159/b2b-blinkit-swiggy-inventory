__pycache__/
*.pyc
.venv/
venv/
*.log
.DS_Store
# keep catalog_lite.db + CSVs (needed at runtime) — only ignore SQLite side files
backend/data/*.db-wal
backend/data/*.db-shm

# Non-shipping frontend variants. frontend/ is served by a WILDCARD StaticFiles mount,
# so anything left in it is publicly reachable -- b2b_v2_static.html is a fake-data
# mockup. Keep alternates here, out of the served directory, not just gitignored.
_archive_frontend/
visits.json
